# Sistema de Idiomas (PT-BR / Inglês) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao Carcará Code um seletor de idioma (PT-BR / English) que traduz toda a interface — telas React e menus/notificações nativas do Electron — sem mistura de idiomas.

**Architecture:** Contexto React caseiro (`LanguageProvider` + hook `useT()`), espelhando `src/lib/theme.jsx`. As traduções vivem em dois JSON (`pt.json` / `en.json`) consumidos pelo renderer; o processo `main.js` tem um arquivo de strings nativas próprio (`main.i18n.cjs`) e recebe a escolha por IPC para reconstruir menus e notificar no idioma certo. Um smoke test garante paridade de chaves entre os idiomas.

**Tech Stack:** React 19, Vite 8, Electron 33, JSON para dicionários, `localStorage` (renderer) + `config.json` em `userData` (main). Sem dependência nova.

## Global Constraints

- **Sem dependência nova.** Solução caseira; nada de i18next/react-intl.
- **Dois idiomas:** `'pt'` e `'en'`. Códigos exatos, em minúsculas.
- **Padrão na 1ª execução:** seguir o sistema — `navigator.language` começando com `pt` → `'pt'`, senão `'en'` (renderer); `app.getLocale()` começando com `pt` → `'pt'`, senão `'en'` (main).
- **Persistência:** `localStorage['lang']` (renderer) e `cfg.language` em `config.json` (main).
- **Regra de tradução PT-BR:** traduzir ao máximo; manter jargão consagrado (`Git`, `commit`, `MCP`, `API`, `Preview`, `terminal`, `DevTools`, `stdio`, `HTTP`) e nomes próprios (`Claude Code`, `Codex`, `OpenCode`, `Antigravity`, `GitHub`, `Carcará Code`) idênticos nos dois idiomas.
- **Fallback nunca quebra a tela:** chave ausente cai para PT e, se ainda faltar, mostra a própria chave.
- **Build obrigatório:** edições em `src/` só aparecem após `npm run build` (o app carrega `dist/`). Não relançar o app à força — ele pode ter uma sessão Claude viva.
- **Smoke test:** `node scripts/i18n-parity.smoke.cjs` deve passar ao fim de toda task que mexe em locale.
- **Commits:** locais apenas; nunca `git push`.

---

## File Structure

**Criados:**
- `src/lib/i18n.jsx` — `LanguageProvider`, `useT()`, `useLang()`, função `t()`.
- `src/lib/locales/pt.json` — dicionário PT-BR (cresce a cada task).
- `src/lib/locales/en.json` — dicionário inglês (mesmas chaves).
- `main.i18n.cjs` — strings nativas do main (objeto `{ pt, en }`), colocado na raiz junto do `main.js`.
- `scripts/i18n-parity.smoke.cjs` — verifica paridade de chaves pt/en.

**Modificados:**
- `src/main.jsx` — envolver o app com `LanguageProvider`.
- `src/components/SettingsModal.jsx` — nova aba "Idioma" + migração das strings.
- `src/components/Rail.jsx`, `src/App.jsx`, `src/components/CommandPalette.jsx`, `src/components/ErrorBoundary.jsx`, `src/components/ui/empty-state.jsx` — migração.
- `src/components/ChatPanel.jsx` — migração.
- `src/components/CodeView.jsx` — migração (inclui o menu de contexto da árvore).
- `src/components/GitPanel.jsx`, `src/components/CheckpointsPanel.jsx`, `src/components/PreviewPanel.jsx` — migração.
- `src/components/MCPPanel.jsx`, `src/components/McpInspectorDrawer.jsx`, `src/components/McpToolForm.jsx`, `src/components/McpServerRequestModal.jsx` — migração.
- `src/components/ApiPanel.jsx`, `src/components/SetupScreen.jsx`, `src/components/XlsxViewer.jsx` — migração.
- `main.js` — IPC `lang:get`/`lang:set`, persistência `cfg.language`, menu de contexto e notificação via `main.i18n.cjs`.
- `preload.js` — expor `getLang`/`setLang`.
- `package.json` — script `test:i18n`.

**Padrão de migração (vale para toda task de componente):**
1. No topo do componente: `import { useT } from '@/lib/i18n';` (ou caminho relativo equivalente ao usado no arquivo).
2. Dentro do componente, antes do `return`: `const t = useT();`
3. Trocar cada string literal de UI por `t('namespace.chave')`. Strings com variável usam `t('chave', { nome })` e o token `{nome}` no JSON.
4. Adicionar as chaves novas em **ambos** `pt.json` e `en.json`.
5. `npm run build`, `node scripts/i18n-parity.smoke.cjs`, conferir, commitar.

> **Para componentes que NÃO são funções de componente React** (ex.: helpers fora do componente, arrays definidos no módulo): não dá pra chamar `useT()`. Nesses casos, ou mova o texto para dentro do componente, ou passe `t` como argumento. Cada task aponta onde isso ocorre.

---

## Task 1: Engine i18n + locales seed + smoke test + wiring

**Files:**
- Create: `src/lib/i18n.jsx`
- Create: `src/lib/locales/pt.json`
- Create: `src/lib/locales/en.json`
- Create: `scripts/i18n-parity.smoke.cjs`
- Modify: `src/main.jsx`
- Modify: `package.json`

**Interfaces:**
- Produces: `LanguageProvider` (componente), `useT()` → `(key: string, vars?: object) => string`, `useLang()` → `{ lang: 'pt'|'en', setLang: (l) => void }`. Importáveis de `@/lib/i18n`.

- [ ] **Step 1: Criar o seed dos dicionários**

`src/lib/locales/pt.json`:
```json
{
  "language": {
    "title": "Idioma",
    "subtitle": "Escolha o idioma da interface. A mudança vale na hora.",
    "pt": "Português (Brasil)",
    "en": "English"
  }
}
```

`src/lib/locales/en.json`:
```json
{
  "language": {
    "title": "Language",
    "subtitle": "Choose the interface language. The change applies instantly.",
    "pt": "Português (Brasil)",
    "en": "English"
  }
}
```

- [ ] **Step 2: Criar a engine `src/lib/i18n.jsx`**

```jsx
import { createContext, useContext, useEffect, useState } from 'react';
import pt from './locales/pt.json';
import en from './locales/en.json';

const DICTS = { pt, en };

// Idioma inicial: localStorage > idioma do sistema > 'pt'.
function detectInitial() {
  const saved = localStorage.getItem('lang');
  if (saved === 'pt' || saved === 'en') return saved;
  const sys = (navigator.language || '').toLowerCase();
  return sys.startsWith('pt') ? 'pt' : 'en';
}

// Resolve 'a.b.c' num objeto aninhado; undefined se faltar.
function resolve(dict, key) {
  return key.split('.').reduce((o, k) => (o == null ? undefined : o[k]), dict);
}

// Troca {tokens} pelos valores em vars.
function interpolate(str, vars) {
  if (!vars) return str;
  return str.replace(/\{(\w+)\}/g, (m, k) => (k in vars ? String(vars[k]) : m));
}

const I18nCtx = createContext({ lang: 'pt', setLang: () => {}, t: (k) => k });

export function LanguageProvider({ children }) {
  const [lang, setLang] = useState(detectInitial);

  useEffect(() => {
    localStorage.setItem('lang', lang);
    document.documentElement.lang = lang === 'pt' ? 'pt-BR' : 'en';
    // Mantém o processo main em sincronia (menus/notificações nativas), inclusive no boot.
    try { window.api?.setLang?.(lang); } catch {}
  }, [lang]);

  // Fallback em cascata: idioma ativo → pt → a própria chave. Nunca lança.
  const t = (key, vars) => {
    const hit = resolve(DICTS[lang], key) ?? resolve(DICTS.pt, key);
    return typeof hit === 'string' ? interpolate(hit, vars) : key;
  };

  return <I18nCtx.Provider value={{ lang, setLang, t }}>{children}</I18nCtx.Provider>;
}

export function useT() { return useContext(I18nCtx).t; }
export function useLang() { const { lang, setLang } = useContext(I18nCtx); return { lang, setLang }; }
```

- [ ] **Step 3: Criar o smoke test `scripts/i18n-parity.smoke.cjs`**

```cjs
// Garante que pt.json e en.json têm exatamente as mesmas chaves (recursivo).
// Rode com: node scripts/i18n-parity.smoke.cjs
const pt = require('../src/lib/locales/pt.json');
const en = require('../src/lib/locales/en.json');

function flatten(obj, prefix = '', out = []) {
  for (const [k, v] of Object.entries(obj)) {
    const key = prefix ? `${prefix}.${k}` : k;
    if (v && typeof v === 'object' && !Array.isArray(v)) flatten(v, key, out);
    else out.push(key);
  }
  return out;
}

let fail = 0;
function compare(label, a, b) {
  const ka = new Set(flatten(a));
  const kb = new Set(flatten(b));
  for (const k of ka) if (!kb.has(k)) { console.error(`  FALTA no ${label} (en): ${k}`); fail++; }
  for (const k of kb) if (!ka.has(k)) { console.error(`  FALTA no ${label} (pt): ${k}`); fail++; }
}

compare('renderer', pt, en);

if (fail) { console.error(`\n${fail} chave(s) divergente(s).`); process.exit(1); }
console.log('i18n parity ok');
```

- [ ] **Step 4: Rodar o smoke test (deve passar)**

Run: `node scripts/i18n-parity.smoke.cjs`
Expected: imprime `i18n parity ok`, exit 0.

- [ ] **Step 5: Envolver o app com o provider em `src/main.jsx`**

Adicionar o import e envolver `<App />`. Resultado de `src/main.jsx`:
```jsx
import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import { ThemeProvider } from './lib/theme.jsx';
import { LanguageProvider } from './lib/i18n.jsx';
import { ErrorBoundary } from './components/ErrorBoundary.jsx';
import './index.css';

window.addEventListener('error', (e) => {
  console.error('GLOBAL ERROR:', e.message, '\n', e.error && e.error.stack);
});

createRoot(document.getElementById('root')).render(
  <ThemeProvider>
    <LanguageProvider>
      <ErrorBoundary label="Carcará Code">
        <App />
      </ErrorBoundary>
    </LanguageProvider>
  </ThemeProvider>
);

function dismissSplash() {
  const s = document.getElementById('splash');
  const root = document.getElementById('root');
  if (!s || !root || root.childElementCount === 0) return;
  s.style.opacity = '0';
  setTimeout(() => s.remove(), 400);
}
requestAnimationFrame(() => requestAnimationFrame(dismissSplash));
```

- [ ] **Step 6: Adicionar script de teste no `package.json`**

Em `"scripts"`, adicionar:
```json
"test:i18n": "node scripts/i18n-parity.smoke.cjs"
```

- [ ] **Step 7: Build e verificação**

Run: `npm run build`
Expected: build conclui sem erro.

- [ ] **Step 8: Commit**

```bash
git add src/lib/i18n.jsx src/lib/locales/pt.json src/lib/locales/en.json scripts/i18n-parity.smoke.cjs src/main.jsx package.json
git commit -m "feat(i18n): engine de idiomas + seed pt/en + smoke de paridade"
```

---

## Task 2: Aba "Idioma" nas Configurações + migração do SettingsModal

**Files:**
- Modify: `src/components/SettingsModal.jsx`
- Modify: `src/lib/locales/pt.json`
- Modify: `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT` e `useLang` de `@/lib/i18n`; namespace `language` (Task 1).
- Produces: namespace `settings` no dicionário.

- [ ] **Step 1: Adicionar o namespace `settings` aos dois JSON**

Adicionar em `pt.json` (e o correspondente em `en.json` da coluna EN):

| chave | PT-BR | EN |
|---|---|---|
| settings.title | Configurações | Settings |
| settings.tabAi | IA por projeto | AI per project |
| settings.tabAppearance | Aparência | Appearance |
| settings.tabCode | Códigos | Code |
| settings.tabNotify | Notificações | Notifications |
| settings.tabDeps | Dependências | Dependencies |
| settings.tabLanguage | Idioma | Language |
| settings.tabAbout | Sobre & créditos | About & credits |
| settings.close | Fechar (Esc) | Close (Esc) |
| settings.aiIntro | Escolha qual CLI de IA cada projeto usa. Vale para as novas sessões abertas naquele projeto. | Choose which AI CLI each project uses. Applies to new sessions opened in that project. |
| settings.aiNewSessions | novas sessões | new sessions |
| settings.aiEmpty | Nenhum projeto ainda. Adicione um projeto na barra lateral. | No projects yet. Add a project from the sidebar. |
| settings.aiCustomPlaceholder | comando do CLI (ex.: meu-cli --flag) | CLI command (e.g., my-cli --flag) |
| settings.aiClaudeDesc | Sua assinatura Claude (não a API). | Your Claude subscription (not the API). |
| settings.aiCodexDesc | ChatGPT/OpenAI no terminal · npm i -g @openai/codex. | ChatGPT/OpenAI in the terminal · npm i -g @openai/codex. |
| settings.aiOpencodeDesc | Open-source · npm i -g opencode-ai. | Open-source · npm i -g opencode-ai. |
| settings.aiAgyDesc | Google · substituiu o Gemini CLI. | Google · replaced the Gemini CLI. |
| settings.aiCustomDesc | Defina o comando manualmente. | Set the command manually. |
| settings.aiCustomLabel | Personalizado | Custom |
| settings.appTheme | Tema da interface | Interface theme |
| settings.themeLight | Claro | Light |
| settings.themeDark | Escuro | Dark |
| settings.zoomTitle | Zoom da interface | Interface zoom |
| settings.zoomHelp | Aumenta ou diminui o tamanho de tudo no app (rail, chat, abas…). Também dá pra usar | Increases or decreases the size of everything in the app (rail, chat, tabs…). You can also use |
| settings.zoomFocusHint | com o foco fora do preview. | with focus outside the preview. |
| settings.zoomOut | Diminuir | Zoom out |
| settings.zoomIn | Aumentar | Zoom in |
| settings.zoomReset | Resetar para 100% | Reset to 100% |
| settings.zoomResetLabel | Resetar | Reset |
| settings.termTitle | Aparência do terminal | Terminal appearance |
| settings.termHelp | Cor de fundo do terminal e do Claude Code. Mantemos o tema da CLI casado com o fundo pra o texto não ficar ilegível no claro. | Background color of the terminal and Claude Code. We keep the CLI theme matched to the background so text stays legible in light mode. |
| settings.termAuto | Acompanhar app | Match app |
| settings.codeAutosaveTitle | Salvar automaticamente | Auto-save |
| settings.codeAutosaveHelp | Salva os arquivos abertos no editor sozinho, pouco depois de você parar de digitar. Com isso ligado você não precisa apertar | Automatically saves open editor files shortly after you stop typing. With this on you don't need to press |
| settings.codeAutosaveHelp2 | — e o Preview já recarrega com a última versão. | — and the Preview reloads with the latest version. |
| settings.autosaveOn | Autosave ligado | Auto-save on |
| settings.autosaveOff | Autosave desligado | Auto-save off |
| settings.notifyTitle | Avisar quando o Claude terminar | Notify when Claude finishes |
| settings.notifyHelp | Mostra uma notificação do sistema quando o Claude Code conclui uma tarefa num projeto que você | Shows a system notification when Claude Code finishes a task in a project you |
| settings.notifyHelpNotLooking | não está olhando | are not looking at |
| settings.notifyHelp2 | . Útil pra deixar rodando num projeto e ir mexer em outro. Vale só pra projetos em Claude Code — Codex/OpenCode/Antigravity/personalizado não disparam aviso. | . Useful to leave running in one project while you work on another. Only applies to Claude Code projects — Codex/OpenCode/Antigravity/custom do not trigger notifications. |
| settings.notifyOn | Notificações ligadas | Notifications on |
| settings.notifyOff | Notificações desligadas | Notifications off |
| settings.depsIntro | Ferramentas que o Carcará usa no seu computador. A gente checa uma vez no primeiro uso — use isto pra verificar de novo caso tenha reinstalado o sistema, formatado uma partição ou removido alguma delas. O CLI de IA (Claude, Codex, OpenCode…) não entra aqui: ele é escolhido por projeto na aba | Tools Carcará uses on your computer. We check once on first use — use this to check again if you reinstalled the system, formatted a partition or removed any of them. The AI CLI (Claude, Codex, OpenCode…) is not here: it's chosen per project on the |
| settings.depsTabRef | IA por projeto | AI per project |
| settings.depsRecheck | Verificar de novo | Check again |
| settings.aboutRole | Professor de IA e Automações | AI & Automation Teacher |
| settings.aboutBlurb | Carcará Code é um projeto independente, feito com carinho por mim. Se te ajudou de alguma forma, vem trocar uma ideia — adoro saber quem está usando. | Carcará Code is an independent project, made with care by me. If it helped you in any way, come say hi — I love knowing who's using it. |
| settings.aboutWhereToFind | Onde me encontrar | Where to find me |
| settings.aboutThanksTitle | Obrigado por usar o Carcará Code | Thanks for using Carcará Code |
| settings.aboutThanksBody | Feito de forma independente por {name}. Sugestões, ideias e feedback são muito bem-vindos — é o que faz o projeto melhorar. | Made independently by {name}. Suggestions, ideas and feedback are very welcome — that's what makes the project better. |

> O sub-`sub` dos links sociais (`Site pessoal`, `E-mail`, `GitHub`, `LinkedIn`, `Instagram`, `YouTube`) também deve virar chave `settings.linkSite/linkEmail/linkGithub/linkLinkedin/linkInstagram/linkYoutube` (PT = EN exceto `Site pessoal`→`Personal site`, `E-mail`→`Email`). `AUTHOR.name` e os `label`/`href` permanecem fixos.

- [ ] **Step 2: Migrar o SettingsModal para `t()`**

No topo: `import { useT, useLang } from '@/lib/i18n';`. Dentro de `SettingsModal`, junto dos outros hooks: `const t = useT(); const { lang, setLang } = useLang();`.

As `AI_OPTIONS` e `AUTHOR` estão fora do componente (escopo de módulo) — não dá pra chamar `useT()` nelas. Tratamento: trocar o campo `desc`/`role`/`blurb`/`sub` por uma **chave** e resolver com `t(opt.desc)` no ponto de render. Ex.: `desc: 'settings.aiClaudeDesc'` e no JSX `title={t(opt.desc)}`. Mesmo para `AUTHOR.role` → `t('settings.aboutRole')`, `AUTHOR.blurb` → `t('settings.aboutBlurb')`, e cada `link.sub` → `t(link.sub)`.

Substituir os literais conforme a tabela do Step 1: o título "Configurações", o map do header (`tab === 'ai' ? '...' : ...`) passa a usar `t('settings.tabAi')` etc., os `<TabButton>` recebem `t('settings.tabAi')`…, e cada bloco de conteúdo (`ai`, `appearance`, `code`, `notify`, `deps`, `about`) tem seus textos trocados.

- [ ] **Step 3: Adicionar a aba "Idioma" na navegação**

Importar `Languages` do lucide (junto dos outros ícones na linha 2; `Globe` já existe e pode ser reusado). Na navegação lateral, depois da aba `deps` e antes do separador `about`, adicionar:
```jsx
<TabButton active={tab === 'language'} onClick={() => setTab('language')} icon={<Globe />}>{t('settings.tabLanguage')}</TabButton>
```
No map do header (linha ~185), incluir o caso `language`:
```jsx
{tab === 'ai' ? t('settings.tabAi')
 : tab === 'code' ? t('settings.tabCode')
 : tab === 'notify' ? t('settings.tabNotify')
 : tab === 'deps' ? t('settings.tabDeps')
 : tab === 'language' ? t('settings.tabLanguage')
 : tab === 'about' ? t('settings.tabAbout')
 : t('settings.tabAppearance')}
```

- [ ] **Step 4: Adicionar o conteúdo da aba "Idioma"**

Depois do bloco `{tab === 'deps' && (...)}` e antes de `{tab === 'about' && (...)}`:
```jsx
{tab === 'language' && (
  <div className="mx-auto max-w-3xl">
    <div className="text-[13px] font-medium">{t('language.title')}</div>
    <p className="mt-1 text-xs text-muted-foreground">{t('language.subtitle')}</p>
    <div className="mt-3 grid max-w-md grid-cols-2 gap-2">
      <button type="button" onClick={() => setLang('pt')}
        className={cn('flex items-center justify-center gap-2 rounded-md border p-3 text-sm transition-colors hover:bg-muted', lang === 'pt' && 'border-primary ring-1 ring-primary')}>
        🇧🇷 {t('language.pt')}
      </button>
      <button type="button" onClick={() => setLang('en')}
        className={cn('flex items-center justify-center gap-2 rounded-md border p-3 text-sm transition-colors hover:bg-muted', lang === 'en' && 'border-primary ring-1 ring-primary')}>
        🇺🇸 {t('language.en')}
      </button>
    </div>
  </div>
)}
```

- [ ] **Step 5: Smoke + build**

Run: `node scripts/i18n-parity.smoke.cjs` → `i18n parity ok`
Run: `npm run build` → sem erro.

- [ ] **Step 6: Verificação visual**

Abrir Configurações → aba Idioma → alternar PT/EN e confirmar que todas as abas e textos das Configurações trocam na hora.

- [ ] **Step 7: Commit**

```bash
git add src/components/SettingsModal.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): aba Idioma + Configurações traduzidas"
```

---

## Task 3: Migração Rail + App + CommandPalette + ErrorBoundary + empty-state

**Files:**
- Modify: `src/components/Rail.jsx`, `src/App.jsx`, `src/components/CommandPalette.jsx`, `src/components/ErrorBoundary.jsx`, `src/components/ui/empty-state.jsx`
- Modify: `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT` de `@/lib/i18n`.
- Produces: namespaces `rail`, `app`, `palette`, `error`.

> **Nota `ErrorBoundary`:** é um **class component** — `useT()` (hook) não funciona dentro dele. Tratamento: `ErrorBoundary` já recebe a prop `label`; para os demais textos, criar um pequeno wrapper funcional que injeta `t` via props, OU resolver os textos no `render()` lendo direto dos JSON com um helper `tStatic(key, lang)` que lê `localStorage['lang']`. Implementação recomendada: adicionar em `src/lib/i18n.jsx` um helper exportado `export function tStatic(key, vars) { const lang = localStorage.getItem('lang') === 'en' ? 'en' : 'pt'; const hit = resolve(DICTS[lang], key) ?? resolve(DICTS.pt, key); return typeof hit === 'string' ? interpolate(hit, vars) : key; }` e usar `tStatic('error.something_broke')` no `render()` do ErrorBoundary. (Aceitável: o idioma do ErrorBoundary só muda no próximo render, o que é suficiente para uma tela de erro.)

- [ ] **Step 1: Adicionar os namespaces aos JSON**

`rail`:
| chave | PT-BR | EN |
|---|---|---|
| rail.search_tooltip | Buscar projetos, arquivos e ações (Ctrl+K) | Search projects, files and actions (Ctrl+K) |
| rail.claude_working | Claude trabalhando… | Claude is working… |
| rail.claude_asking | Claude pediu uma confirmação | Claude is requesting confirmation |
| rail.claude_done | Claude terminou | Claude finished |
| rail.add_project_tooltip | Adicionar projeto(s) — pode escolher várias pastas | Add project(s) — you can choose multiple folders |
| rail.settings_tooltip | Configurações | Settings |
| rail.menu_restart_running | Reiniciar servidor | Restart server |
| rail.menu_start_running | Iniciar servidor | Start server |
| rail.menu_stop_server | Parar servidor | Stop server |
| rail.menu_remove_project | Remover da lista | Remove from list |

`app`:
| chave | PT-BR | EN |
|---|---|---|
| app.no_project_selected | Selecione um projeto | Select a project |
| app.restart_server_btn | Reiniciar | Restart |
| app.stop_server_btn | Parar | Stop |
| app.restart_server_tooltip | Reiniciar servidor | Restart server |
| app.stop_server_tooltip | Parar servidor | Stop server |
| app.collapse_chat_tooltip | Recolher chat | Collapse chat |
| app.expand_chat_tooltip | Expandir chat | Expand chat |
| app.cmd_group_projects | Projetos | Projects |
| app.cmd_hint_current | atual | current |
| app.cmd_hint_open_project | abrir projeto | open project |
| app.cmd_view_preview | Preview | Preview |
| app.cmd_hint_tab | aba | tab |
| app.cmd_view_code | Código | Code |
| app.cmd_view_git | Git | Git |
| app.cmd_view_history | Histórico (checkpoints) | History (checkpoints) |
| app.cmd_view_api | API | API |
| app.cmd_view_mcp | MCP | MCP |
| app.cmd_view_board | Quadro | Board |
| app.cmd_group_server | Servidor | Server |
| app.cmd_restart_server | Reiniciar servidor | Restart server |
| app.cmd_hint_preview | preview | preview |
| app.cmd_stop_server | Parar servidor | Stop server |
| app.cmd_group_chat | Chat | Chat |
| app.cmd_new_chat_session | Nova sessão de chat | New chat session |
| app.cmd_toggle_chat | Recolher/expandir chat | Collapse/expand chat |
| app.cmd_group_app | App | App |
| app.cmd_add_project | Adicionar projeto… | Add project… |
| app.cmd_toggle_theme | Alternar tema claro/escuro | Toggle light/dark theme |
| app.cmd_settings | Configurações | Settings |
| app.cmd_setup_tools | Preparar meu PC (ferramentas) | Set up my PC (tools) |
| app.cmd_reload_app | Recarregar o app | Reload the app |
| app.remove_project_title | Remover projeto | Remove project |
| app.remove_project_message | Remover "{name}" da lista? O projeto no disco NÃO é apagado. | Remove "{name}" from the list? The project on disk is NOT deleted. |
| app.cancel_button | Cancelar | Cancel |
| app.remove_button | Remover | Remove |

`palette`:
| chave | PT-BR | EN |
|---|---|---|
| palette.search_placeholder | Buscar comandos e arquivos… | Search commands and files… |
| palette.no_results | Nenhum resultado. | No results. |
| palette.group_files | Arquivos | Files |
| palette.group_actions | Ações | Actions |

`error`:
| chave | PT-BR | EN |
|---|---|---|
| error.something_broke | Algo quebrou neste painel | Something broke in this panel |
| error.panel_label | Painel | Panel |
| error.rest_of_app_works | O resto do app continua funcionando — você pode trocar de aba e seguir trabalhando. Tente novamente ou copie o erro pra investigar. | The rest of the app keeps working — you can switch tabs and continue. Try again or copy the error to investigate. |
| error.code_label | Código | Code |
| error.unknown_error | Erro desconhecido | Unknown error |
| error.technical_details | Detalhes técnicos | Technical details |
| error.copy_error_button | Copiar erro | Copy error |
| error.copied_button | Copiado! | Copied! |
| error.reload_button | Recarregar | Reload |
| error.retry_button | Tentar novamente | Try again |

> `empty-state.jsx`: os `alt=` das imagens são vazios/semânticos — **nenhuma string a traduzir**. Confirmar que não há texto antes de fechar a task.

- [ ] **Step 2: Migrar Rail.jsx, App.jsx, CommandPalette.jsx (com `useT()`)**

Em cada um: `import { useT } from '@/lib/i18n';` + `const t = useT();` dentro do componente, e trocar cada literal pela chave correspondente da tabela. Strings com variável (`app.remove_project_message`) usam `t('app.remove_project_message', { name })`. Os comandos da paleta definidos em App (grupos/labels/hints) trocam para `t('app.cmd_...')`.

- [ ] **Step 3: Migrar ErrorBoundary.jsx via `tStatic`**

Adicionar o helper `tStatic` em `src/lib/i18n.jsx` (ver nota acima) e usá-lo no `render()`. A prop `label` recebida segue como fallback do nome do painel.

- [ ] **Step 4: Smoke + build + verificação**

Run: `node scripts/i18n-parity.smoke.cjs` → ok
Run: `npm run build` → sem erro
Verificar visualmente o rail (tooltips), a paleta (Ctrl+K), o diálogo de remover projeto, e (forçando um erro) o ErrorBoundary nos dois idiomas.

- [ ] **Step 5: Commit**

```bash
git add src/components/Rail.jsx src/App.jsx src/components/CommandPalette.jsx src/components/ErrorBoundary.jsx src/lib/i18n.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): rail, app, paleta de comandos e error boundary"
```

---

## Task 4: Migração ChatPanel

**Files:**
- Modify: `src/components/ChatPanel.jsx`, `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT`. Produces: namespaces `prompts`, `session`, `chat`.

> **Fora do escopo:** o conteúdo gerado pelo Claude/terminal dentro do chat **não** é traduzido (vem do xterm/CLI).

- [ ] **Step 1: Adicionar as chaves aos JSON**

`prompts`:
| chave | PT-BR | EN |
|---|---|---|
| prompts.library_title | Biblioteca de prompts | Prompt library |
| prompts.back_to_selection | Voltar à seleção rápida | Back to quick selection |
| prompts.manage_title | Gerenciar prompts | Manage prompts |
| prompts.close | Fechar (Esc) | Close (Esc) |
| prompts.search_placeholder | Buscar prompts… | Search prompts… |
| prompts.count_single | prompt | prompt |
| prompts.count_plural | prompts | prompts |
| prompts.new | Novo | New |
| prompts.empty | Nenhum prompt salvo ainda. | No prompts saved yet. |
| prompts.no_results | Nada encontrado para "{query}". | Nothing found for "{query}". |
| prompts.pin | Fixar no topo | Pin to top |
| prompts.unpin | Desafixar | Unpin |
| prompts.edit | Editar | Edit |
| prompts.remove | Remover | Remove |
| prompts.insert | Inserir no chat | Insert in chat |
| prompts.editor_title_edit | Editar prompt | Edit prompt |
| prompts.editor_title_new | Novo prompt | New prompt |
| prompts.editor_title_placeholder | Título (opcional) | Title (optional) |
| prompts.editor_body_placeholder | Prompt — ex.: rode os testes e corrija o que quebrar | Prompt — e.g.: run the tests and fix what breaks |
| prompts.editor_cancel | Cancelar edição | Cancel editing |
| prompts.editor_save | Salvar | Save |
| prompts.editor_add | Adicionar | Add |
| prompts.confirm_title | Remover prompt | Remove prompt |
| prompts.confirm_message | Remover "{title}"? Esta ação não pode ser desfeita. | Remove "{title}"? This action cannot be undone. |
| prompts.confirm_cancel | Cancelar | Cancel |
| prompts.confirm_delete | Remover | Remove |
| prompts.picker_search_placeholder | Buscar prompt… (Enter insere) | Search prompt… (Enter to insert) |
| prompts.picker_manage_tooltip | Gerenciar / editar prompts | Manage / edit prompts |
| prompts.picker_manage_button | Gerenciar | Manage |
| prompts.picker_empty | Nenhum prompt salvo ainda. | No prompts saved yet. |
| prompts.picker_no_results | Nenhum resultado. | No results. |
| prompts.picker_create | Criar um prompt | Create a prompt |
| prompts.picker_open | Abrir gerenciador | Open manager |
| prompts.picker_help | ↑↓ navegar · Enter inserir · Esc fechar | ↑↓ navigate · Enter insert · Esc close |

`session`:
| chave | PT-BR | EN |
|---|---|---|
| session.activity_working | Claude trabalhando… | Claude is working… |
| session.activity_asking | Claude pediu uma confirmação | Claude is requesting confirmation |
| session.activity_waiting | Claude aguardando você | Claude is waiting for you |
| session.tabs_close | Fechar sessão | Close session |
| session.new | Nova sessão do Claude Code | New Claude Code session |

`chat`:
| chave | PT-BR | EN |
|---|---|---|
| chat.empty | Clique num projeto pra abrir o Claude Code aqui. | Click a project to open Claude Code here. |

- [ ] **Step 2: Migrar ChatPanel.jsx**

`import { useT } from '@/lib/i18n';` + `const t = useT();`. Trocar cada literal pelas chaves. Plural de contagem: usar `t(count === 1 ? 'prompts.count_single' : 'prompts.count_plural')`. Strings com variável usam `{ query }` / `{ title }`.

- [ ] **Step 3: Smoke + build + verificação visual** (biblioteca de prompts, abas de sessão, estado vazio do chat) nos dois idiomas.

- [ ] **Step 4: Commit**

```bash
git add src/components/ChatPanel.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): painel de chat (biblioteca de prompts, sessões)"
```

---

## Task 5: Migração CodeView (inclui o menu de contexto da árvore)

**Files:**
- Modify: `src/components/CodeView.jsx`, `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT`. Produces: namespaces `contextMenu`, `tree`, `code`, `env`, `image`, `delete`, `create`, `dialog`.

> Este é o caso da print do usuário: o menu de contexto da árvore (`Reveal in File Explorer`, `Cut`, `Copy`, `Copy Path`, `Rename`, `Delete`, `Paste`) está **em inglês hoje** e precisa ficar PT/EN conforme o idioma.

- [ ] **Step 1: Adicionar as chaves aos JSON**

`contextMenu`:
| chave | PT-BR | EN |
|---|---|---|
| contextMenu.newFile | Novo arquivo | New file |
| contextMenu.newFolder | Nova pasta | New folder |
| contextMenu.paste | Colar | Paste |
| contextMenu.reveal | Abrir no Explorador | Reveal in File Explorer |
| contextMenu.cut | Cortar | Cut |
| contextMenu.copy | Copiar | Copy |
| contextMenu.copyPath | Copiar caminho | Copy path |
| contextMenu.rename | Renomear | Rename |
| contextMenu.delete_single | Excluir | Delete |
| contextMenu.delete_multiple | Excluir {count} itens | Delete {count} items |

`tree`:
| chave | PT-BR | EN |
|---|---|---|
| tree.search_placeholder | Buscar arquivos… | Search files… |
| tree.search_clear | Limpar | Clear |
| tree.no_results | Nenhum arquivo encontrado. | No files found. |
| tree.empty | Abra um projeto. | Open a project. |
| tree.link_title | {name} — atalho (abre no Explorador de Arquivos) | {name} — shortcut (opens in File Explorer) |

`code`:
| chave | PT-BR | EN |
|---|---|---|
| code.tabs_select_file | Selecione um arquivo | Select a file |
| code.tabs_close | Fechar | Close |
| code.env_raw_show_tip | Ver como texto puro | View as plain text |
| code.env_raw_hide_tip | Voltar ao editor seguro (mascarado) | Back to secure editor (masked) |
| code.env_raw_show_btn | Ver como texto | View as text |
| code.env_raw_hide_btn | Modo seguro | Secure mode |
| code.md_toggle_edit | Editar o markdown | Edit the markdown |
| code.md_toggle_preview | Visualizar renderizado | View rendered |
| code.md_button_edit | Editar | Edit |
| code.md_button_preview | Visualizar | Preview |
| code.save_button | Salvar | Save |
| code.save_tooltip | Salvar (Ctrl+S) | Save (Ctrl+S) |
| code.loading_spreadsheet | Carregando planilha… | Loading spreadsheet… |
| code.loading_preview | Carregando preview… | Loading preview… |
| code.empty | Selecione um arquivo na árvore. | Select a file from the tree. |
| code.binary_notice | Arquivo binário — não editável. | Binary file — not editable. |
| code.error_notice | Erro: | Error: |

`delete`:
| chave | PT-BR | EN |
|---|---|---|
| delete.confirm_title | Mover para a Lixeira | Move to Trash |
| delete.confirm_message_single | Tem certeza que deseja mover "{name}" para a Lixeira? | Are you sure you want to move "{name}" to Trash? |
| delete.confirm_message_multiple | Tem certeza que deseja mover {count} itens para a Lixeira? | Are you sure you want to move {count} items to Trash? |
| delete.confirm_button | Mover para a Lixeira | Move to Trash |
| delete.confirm_cancel | Cancelar | Cancel |

`create`:
| chave | PT-BR | EN |
|---|---|---|
| create.file_title | Novo arquivo | New file |
| create.folder_title | Nova pasta | New folder |
| create.file_placeholder | nome-do-arquivo.txt | filename.txt |
| create.folder_placeholder | nome-da-pasta | folder-name |
| create.button | Criar | Create |
| create.cancel | Cancelar | Cancel |

`env`:
| chave | PT-BR | EN |
|---|---|---|
| env.editor_title | Editor seguro de ambiente — valores mascarados | Secure environment editor — values masked |
| env.reveal_all | Revelar tudo | Reveal all |
| env.hide_all | Ocultar tudo | Hide all |
| env.empty | Nenhuma variável. Clique em "Adicionar variável". | No variables. Click "Add variable". |
| env.variable_remove | Remover variável | Remove variable |
| env.variable_add | Adicionar variável | Add variable |
| env.toggle_reveal | Revelar | Reveal |
| env.toggle_hide | Ocultar | Hide |

`image`:
| chave | PT-BR | EN |
|---|---|---|
| image.zoom_out | Diminuir | Zoom out |
| image.zoom_in | Aumentar | Zoom in |
| image.fit | Ajustar à tela | Fit to screen |
| image.help | role para zoom · arraste para mover · duplo-clique reseta | scroll to zoom · drag to pan · double-click resets |

`dialog`:
| chave | PT-BR | EN |
|---|---|---|
| dialog.cancel | Cancelar | Cancel |
| dialog.confirm | OK | OK |

- [ ] **Step 2: Migrar CodeView.jsx**

`import { useT } from '@/lib/i18n';` + `const t = useT();`. Atenção a dois pontos onde o texto pode estar fora do componente de render: o `FileMenu`/menu de contexto e o `PromptDialog`. Se estiverem definidos como subcomponentes funcionais, chamar `useT()` dentro deles; se forem arrays/objetos construídos no corpo do componente principal, montar os labels com `t(...)` ali. Plurais: `t(delCount === 1 ? 'contextMenu.delete_single' : 'contextMenu.delete_multiple', { count: delCount })` e equivalente em delete/message. Variáveis `{name}` no link da árvore e nas confirmações.

- [ ] **Step 3: Smoke + build + verificação visual**

Verificar especialmente: **menu de contexto da árvore** (botão direito num arquivo) nos dois idiomas — é o caso reportado; criar/excluir arquivo; editor `.env`; visualizador de imagem.

- [ ] **Step 4: Commit**

```bash
git add src/components/CodeView.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): CodeView + menu de contexto da arvore"
```

---

## Task 6: Migração GitPanel + CheckpointsPanel + PreviewPanel

**Files:**
- Modify: `src/components/GitPanel.jsx`, `src/components/CheckpointsPanel.jsx`, `src/components/PreviewPanel.jsx`, `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT`. Produces: namespaces `git`, `checkpoint`, `preview`.

- [ ] **Step 1: Adicionar as chaves aos JSON**

`git`:
| chave | PT-BR | EN |
|---|---|---|
| git.open_project | Abra um projeto para ver o Git aqui. | Open a project to see Git here. |
| git.not_installed | O Git não está instalado neste computador. | Git is not installed on this computer. |
| git.not_installed_help | A aba Git usa o Git do sistema. Instale uma vez, reabra o Carcará e ela passa a funcionar. | The Git tab uses your system's Git. Install it once, reopen Carcará and it will work. |
| git.download_git | Baixar o Git | Download Git |
| git.git_error | Erro do Git: {error} | Git error: {error} |
| git.not_repository | Este projeto ainda não é um repositório Git. | This project is not a Git repository yet. |
| git.init_repository | Inicializar Git | Initialize Git |
| git.remote_url_placeholder | URL do GitHub (opcional) | GitHub URL (optional) |
| git.connect | Conectar | Connect |
| git.no_changes | Nenhuma alteração. | No changes. |
| git.status_modified | Modificado | Modified |
| git.status_added | Adicionado | Added |
| git.status_deleted | Excluído | Deleted |
| git.status_renamed | Renomeado | Renamed |
| git.status_copied | Copiado | Copied |
| git.status_conflict | Conflito | Conflict |
| git.status_untracked | Novo (não rastreado) | Untracked |
| git.unstage_file | Tirar do stage | Unstage |
| git.stage_file | Adicionar ao stage | Stage |
| git.commit_message | Mensagem do commit | Commit message |
| git.commit_message_staged | Mensagem do commit ({count} em stage) | Commit message ({count} staged) |
| git.commit_message_changes | Mensagem do commit ({count} alterações) | Commit message ({count} changes) |
| git.commit_all_button | Commit de tudo | Commit all |
| git.commit_button | Commit | Commit |
| git.publish_branch | Publicar branch no GitHub | Publish branch to GitHub |
| git.push_commits | Enviar {count} commits pro GitHub | Push {count} commits to GitHub |
| git.pull | Pull | Pull |
| git.push | Push | Push |
| git.refresh | Atualizar | Refresh |
| git.unstage_all | Tirar tudo do stage | Unstage all |
| git.stage_all | Adicionar tudo ao stage | Stage all |
| git.new_branch | nova branch | new branch |
| git.create_branch | Criar | Create |
| git.close_file | fechar | close |
| git.new_file | (novo) | (new) |
| git.error_git | Erro no Git | Git error |
| git.copy_error | Copiar erro | Copy error |
| git.copied | Copiado | Copied |
| git.copy | Copiar | Copy |
| git.close | Fechar | Close |
| git.staged | Em stage | Staged |
| git.changes | Alterações | Changes |

`checkpoint`:
| chave | PT-BR | EN |
|---|---|---|
| checkpoint.history | Histórico | History |
| checkpoint.auto_tooltip | Criar um checkpoint automaticamente quando o Claude termina um turno | Automatically create a checkpoint when Claude finishes a turn |
| checkpoint.auto | Auto | Auto |
| checkpoint.create | Criar | Create |
| checkpoint.refresh | Atualizar | Refresh |
| checkpoint.none | Sem checkpoints ainda | No checkpoints yet |
| checkpoint.none_help | Um checkpoint é criado quando o Claude termina um turno, ou clique em "Criar". Dá pra voltar a qualquer um deles. | A checkpoint is created when Claude finishes a turn, or click "Create". You can go back to any of them. |
| checkpoint.restore | Voltar | Restore |
| checkpoint.restore_tooltip | Restaurar o projeto para este ponto | Restore the project to this point |
| checkpoint.newest | · mais recente | · most recent |
| checkpoint.confirm_title | Voltar no tempo | Go back in time |
| checkpoint.confirm_message | Restaurar os arquivos do projeto para {ago}? Um checkpoint do estado atual é salvo antes — você pode voltar. | Restore the project files to {ago}? A checkpoint of the current state is saved first — you can revert. |
| checkpoint.cancel | Cancelar | Cancel |
| checkpoint.confirm | Restaurar | Restore |
| checkpoint.created | Checkpoint criado | Checkpoint created |
| checkpoint.create_error | Falha ao criar checkpoint: {error} | Failed to create checkpoint: {error} |
| checkpoint.restored | Projeto restaurado para o checkpoint | Project restored to checkpoint |
| checkpoint.restore_error | Falha ao restaurar: {error} | Failed to restore: {error} |

`preview`:
| chave | PT-BR | EN |
|---|---|---|
| preview.tab | Preview | Preview |
| preview.code | Código | Code |
| preview.git | Git | Git |
| preview.more_tools | Mais ferramentas (Histórico, API, MCP, Quadro) | More tools (History, API, MCP, Board) |
| preview.history | Histórico | History |
| preview.api | API | API |
| preview.mcp | MCP | MCP |
| preview.board | Quadro | Board |
| preview.back | Voltar | Back |
| preview.forward | Avançar | Forward |
| preview.reload | Recarregar página | Reload page |
| preview.url_placeholder | Preview | Preview |
| preview.open_browser | Abrir no navegador padrão | Open in default browser |
| preview.grab_element | Selecionar elemento (copia pro clipboard) | Select element (copies to clipboard) |
| preview.devtools | DevTools (F12) | DevTools (F12) |
| preview.open_terminal | Abrir terminal | Open terminal |
| preview.close_terminal | Fechar terminal | Close terminal |
| preview.terminal_label | Terminal — {projectName} | Terminal — {projectName} |
| preview.grab_active | Clique num elemento para copiar · Esc cancela | Click an element to copy · Esc to cancel |
| preview.grab_done | ✓ Elemento copiado! | ✓ Element copied! |
| preview.no_preview | O servidor de preview deste projeto não está aberto. Clique em "Reiniciar", no topo, para abri-lo. | The preview server for this project is not open. Click "Restart" at the top to open it. |
| preview.no_preview_server | Este projeto não tem um servidor de preview para abrir. | This project has no preview server to open. |
| preview.copy_prompt | Não subiu? Copiar prompt pro Claude | Didn't load? Copy a prompt for Claude |
| preview.prompt_copied | Copiado! | Copied! |
| preview.select_project | Selecione um projeto para começar a utilizar. | Select a project to get started. |

> **Logs/prompt do Preview** (`preview.claudePrompt*`, mensagens `> reiniciando servidor...` etc.): são textos que o app injeta no terminal/prompt. Traduza-os também, criando chaves `preview.log_restarting`, `preview.log_preparing`, `preview.log_exited`, `preview.log_found`, e `preview.claude_prompt_*` (uma chave por linha, conforme a extração). Mantêm os tokens (`{projectName}`) e quebras `\n`.

- [ ] **Step 2: Migrar os três componentes** com `useT()` + `const t = useT();`. Plurais de commits/alterações: usar a forma com `{count}` (PT já flexiona no texto da chave). Variáveis `{error}`, `{count}`, `{ago}`, `{projectName}`.

- [ ] **Step 3: Smoke + build + verificação visual** (aba Git com e sem repo; checkpoints; preview vazio e ativo) nos dois idiomas.

- [ ] **Step 4: Commit**

```bash
git add src/components/GitPanel.jsx src/components/CheckpointsPanel.jsx src/components/PreviewPanel.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): Git, checkpoints e Preview"
```

---

## Task 7: Migração da suíte MCP

**Files:**
- Modify: `src/components/MCPPanel.jsx`, `src/components/McpInspectorDrawer.jsx`, `src/components/McpToolForm.jsx`, `src/components/McpServerRequestModal.jsx`, `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT`. Produces: namespaces `mcp` (sub-objetos `panel`, `inspector`, `form`, `modal`).

> Termos `MCP`, `OAuth`, `stdio`, `HTTP`, `Bearer`, `Tools`, `Resources`, `Prompts`, `Templates`, `Servers`, `stderr` ficam idênticos nos dois idiomas (jargão/protocolo). Só os rótulos com bom equivalente PT mudam.

- [ ] **Step 1: Adicionar as chaves aos JSON**

`mcp.panel`:
| chave | PT-BR | EN |
|---|---|---|
| mcp.panel.cmd_placeholder | comando (ex.: npx) | command (e.g., npx) |
| mcp.panel.args_placeholder | args (ex.: -y @servidor/mcp) | args (e.g., -y @server/mcp) |
| mcp.panel.url_placeholder | https://servidor.com/mcp | https://server.com/mcp |
| mcp.panel.bearer_placeholder | Bearer token (opcional) | Bearer token (optional) |
| mcp.panel.bearer_title | Enviado como header Authorization: Bearer …. Não é salvo em disco. | Sent as an Authorization: Bearer header. Not saved to disk. |
| mcp.panel.save_btn | Salvar | Save |
| mcp.panel.save_btn_title | Salvar "{name}" | Save "{name}" |
| mcp.panel.save_btn_default_title | Salvar servidor | Save server |
| mcp.panel.disconnect_btn | Desconectar | Disconnect |
| mcp.panel.connect_btn | Conectar | Connect |
| mcp.panel.no_token_oauth_hint | Sem token → Conectar abre o login no navegador (OAuth). | No token → Connect opens browser login (OAuth). |
| mcp.panel.forget_login | Esquecer login | Forget login |
| mcp.panel.advanced | Avançado | Advanced |
| mcp.panel.env_vars_label | Variáveis de ambiente | Environment variables |
| mcp.panel.env_key_placeholder | CHAVE | KEY |
| mcp.panel.env_value_placeholder | valor | value |
| mcp.panel.add_env_var | + adicionar variável | + add variable |
| mcp.panel.timeout_label | Timeout de request (ms): | Request timeout (ms): |
| mcp.panel.timeout_placeholder | padrão 60000 | default 60000 |
| mcp.panel.roots_label | Roots (pastas expostas ao servidor) | Roots (folders exposed to the server) |
| mcp.panel.root_path_placeholder | C:\caminho\pasta ou file:///… | C:\path\folder or file:///… |
| mcp.panel.add_root_path | + adicionar pasta | + add folder |
| mcp.panel.name_label | Nome: | Name: |
| mcp.panel.name_placeholder | ex.: everything | e.g., everything |
| mcp.panel.save_name_btn | Salvar | Save |
| mcp.panel.cancel_btn | Cancelar | Cancel |
| mcp.panel.failed_to_connect | Falha ao conectar. | Failed to connect. |
| mcp.panel.connecting | Conectando… | Connecting… |
| mcp.panel.awaiting_auth | Aguardando autorização no navegador… | Awaiting authorization in the browser… |
| mcp.panel.disconnected | Desconectado | Disconnected |
| mcp.panel.select_item | Selecione um item à esquerda. | Select an item on the left. |
| mcp.panel.invoke_btn | Invocar | Invoke |
| mcp.panel.get_prompt_btn | Obter | Get |
| mcp.panel.no_template_vars | Template sem variáveis. | Template has no variables. |
| mcp.panel.read_btn | Ler | Read |
| mcp.panel.unsubscribe_btn | Cancelar assinatura | Unsubscribe |
| mcp.panel.subscribe_btn | Assinar atualizações | Subscribe to updates |
| mcp.panel.result_error | ERRO | ERROR |
| mcp.panel.result_is_error | RESULTADO (isError) | RESULT (isError) |
| mcp.panel.result | RESULTADO | RESULT |
| mcp.panel.empty_connect | Conecte a um servidor MCP para inspecionar tools, resources e prompts. | Connect to an MCP server to inspect tools, resources and prompts. |
| mcp.panel.nothing_here | Nada aqui. | Nothing here. |
| mcp.panel.templates | Templates | Templates |
| mcp.panel.servers | Servers | Servers |
| mcp.panel.new_server | Novo (limpar nome) | New (clear name) |
| mcp.panel.open_project_to_save | Abra um projeto para salvar servidores. | Open a project to save servers. |
| mcp.panel.no_servers_saved | Nenhum servidor salvo ainda. | No servers saved yet. |
| mcp.panel.delete_server_title | Excluir | Delete |
| mcp.panel.subscribed | assinado | subscribed |

`mcp.inspector`:
| chave | PT-BR | EN |
|---|---|---|
| mcp.inspector.msgs | msgs | msgs |
| mcp.inspector.truncated | truncadas | truncated |
| mcp.inspector.payload_large | payload grande ({size}KB) — clique para renderizar | large payload ({size}KB) — click to render |
| mcp.inspector.no_traffic | Sem tráfego ainda. | No traffic yet. |
| mcp.inspector.clear | Limpar | Clear |
| mcp.inspector.level_label | Nível: | Level: |
| mcp.inspector.server_no_logging | servidor não anuncia logging | server does not announce logging |
| mcp.inspector.no_logs | Sem logs ainda. | No logs yet. |
| mcp.inspector.no_progress | Nenhuma operação com progresso. | No operations in progress. |

> `Inspector`, `History`, `Logging`, `Progress`, `Ping`, `all/request/response/notification`, `setLevel…`, `stderr` ficam iguais.

`mcp.form`:
| chave | PT-BR | EN |
|---|---|---|
| mcp.form.no_args | Esta tool não recebe argumentos. | This tool takes no arguments. |
| mcp.form.select_placeholder | Selecione… | Select… |

`mcp.modal`:
| chave | PT-BR | EN |
|---|---|---|
| mcp.modal.sampling_title | O servidor pediu uma resposta de IA (sampling) | The server requested an AI response (sampling) |
| mcp.modal.sampling_explain | O servidor MCP quer que o cliente gere uma resposta. Como não há IA embutida, você responde manualmente — revise o contexto e escreva a resposta a devolver. | The MCP server wants the client to generate a response. Since there's no built-in AI, you respond manually — review the context and write the response to return. |
| mcp.modal.system_prompt | System | System |
| mcp.modal.messages | Mensagens | Messages |
| mcp.modal.your_response | Sua resposta | Your response |
| mcp.modal.your_response_max | Sua resposta (máx ~{maxTokens} tokens) | Your response (max ~{maxTokens} tokens) |
| mcp.modal.write_placeholder | Escreva a resposta que será devolvida ao servidor… | Write the response that will be returned to the server… |
| mcp.modal.decline | Recusar | Decline |
| mcp.modal.sampling_declined | Sampling recusado pelo usuário. | Sampling declined by the user. |
| mcp.modal.send_response | Enviar resposta | Send response |
| mcp.modal.elicitation_title | O servidor pediu informações (elicitation) | The server requested information (elicitation) |
| mcp.modal.cancel | Cancelar | Cancel |
| mcp.modal.send | Enviar | Send |

- [ ] **Step 2: Migrar os quatro componentes** com `useT()`. `your_response`: usar `t(maxTokens ? 'mcp.modal.your_response_max' : 'mcp.modal.your_response', { maxTokens })`. `payload_large`: `{ size }`. `save_btn_title`: `t(name ? 'mcp.panel.save_btn_title' : 'mcp.panel.save_btn_default_title', { name })`.

- [ ] **Step 3: Smoke + build + verificação visual** (painel MCP, inspector, form de tool, modal de sampling/elicitation) nos dois idiomas.

- [ ] **Step 4: Commit**

```bash
git add src/components/MCPPanel.jsx src/components/McpInspectorDrawer.jsx src/components/McpToolForm.jsx src/components/McpServerRequestModal.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): suite MCP (painel, inspector, form, modal)"
```

---

## Task 8: Migração ApiPanel + SetupScreen + XlsxViewer

**Files:**
- Modify: `src/components/ApiPanel.jsx`, `src/components/SetupScreen.jsx`, `src/components/XlsxViewer.jsx`, `src/lib/locales/pt.json`, `src/lib/locales/en.json`

**Interfaces:**
- Consumes: `useT`. Produces: namespaces `api`, `setup`, `xlsx`.

> `SetupScreen` exporta também `DependencyCards`/`useDependencyStatus` usados pelo SettingsModal. As listas de dependências (`name`/`desc`/`note`) ficam em escopo de módulo — aplicar o mesmo padrão do Task 2: trocar por **chaves** e resolver com `t(dep.desc)` no render.

- [ ] **Step 1: Adicionar as chaves aos JSON**

`api`:
| chave | PT-BR | EN |
|---|---|---|
| api.toggle_tip | Ativar/desativar | Toggle |
| api.remove_tip | Remover | Remove |
| api.add_button | Adicionar | Add |
| api.placeholder_param | Parâmetro | Parameter |
| api.placeholder_value | Valor | Value |
| api.placeholder_header | Header | Header |
| api.url_placeholder | https://api.exemplo.com/recurso | https://api.example.com/resource |
| api.clear_button | Limpar | Clear |
| api.clear_tip | Limpar tudo e recomeçar | Clear everything and start over |
| api.import_button | Importar | Import |
| api.import_tip | Importar de um comando cURL (copiado da documentação da API) | Import from a cURL command (copied from the API docs) |
| api.copy_button | Copiar | Copy |
| api.copy_button_done | Copiado! | Copied! |
| api.copy_tip | Copiar como código | Copy as code |
| api.send_button | Enviar | Send |
| api.send_tip | Enviar (salva no histórico automaticamente) | Send (saves to history automatically) |
| api.import_bar_label | Colar cURL: | Paste cURL: |
| api.import_bar_placeholder | Cole aqui o comando que você copiou da documentação da API | Paste the command you copied from the API docs here |
| api.import_bar_confirm | Importar | Import |
| api.import_bar_cancel | Cancelar | Cancel |
| api.import_error | Não consegui entender esse comando. Confira se você copiou um cURL completo. | I couldn't understand that command. Make sure you copied a complete cURL. |
| api.tabs_params | Params | Params |
| api.tabs_headers | Headers | Headers |
| api.tabs_body | Body | Body |
| api.body_placeholder | { "exemplo": true } | { "example": true } |
| api.response_label | Resposta | Response |
| api.response_loading | Enviando… | Sending… |
| api.response_empty | Envie uma request para ver a resposta aqui. | Send a request to see the response here. |
| api.history_label | Histórico | History |
| api.history_new_call | Nova chamada | New call |
| api.history_clear_all | Limpar todo o histórico | Clear all history |
| api.history_empty | Cada chamada que você enviar aparece aqui, com o resultado. | Each call you send appears here, with its result. |
| api.history_delete | Excluir | Delete |
| api.history_error | erro | error |
| api.toast_success | Pronto! Sua chamada foi montada ✨ | Done! Your call is ready ✨ |

> Os labels de snippet (`cURL`, `JavaScript — fetch`, `Node — fetch`, `Node — axios`, `Python — requests`, `Go`, `PHP`) são nomes técnicos — manter idênticos; podem ficar literais (sem chave) ou em `api.snippet_*` com PT=EN.

`setup`:
| chave | PT-BR | EN |
|---|---|---|
| setup.level_essential | Essencial | Essential |
| setup.level_recommended | Recomendado | Recommended |
| setup.node_desc | Faz o Preview funcionar — ver o projeto rodando ao vivo é o coração do Carcará. Sem Node, sem Preview. | Makes Preview work — seeing the project run live is the heart of Carcará. No Node, no Preview. |
| setup.node_note | Baixe a versão LTS. | Download the LTS version. |
| setup.git_desc | Usado pela aba Git (commits, branches, GitHub) e pela ferramenta Bash do Claude. Sem ele, o Claude cai no PowerShell. | Used by the Git tab (commits, branches, GitHub) and Claude's Bash tool. Without it, Claude falls back to PowerShell. |
| setup.pill_checking | verificando… | checking… |
| setup.pill_installed | Instalado | Installed |
| setup.pill_not_found | Não encontrado | Not found |
| setup.badge | Primeiro uso | First time |
| setup.title_main | Vamos preparar seu PC | Let's set up your PC |
| setup.description | O Carcará abre sozinho, mas ele trabalha em cima de algumas ferramentas que ficam no seu computador. Confira o que já está pronto e instale o que faltar — é uma vez só. | Carcará opens by itself, but it works on top of some tools that live on your computer. Check what's ready and install what's missing — just once. |
| setup.check_again | Verificar de novo | Check again |
| setup.ready_enter | Tudo pronto — entrar | All set — go in |
| setup.continue_anyway | Continuar mesmo assim | Continue anyway |
| setup.footer | Pode entrar e instalar o resto quando quiser. Acabou de instalar algo? Clique em "Verificar de novo". Esta tela não volta mais sozinha — pra checar de novo, vá em Configurações → Dependências (ou Ctrl/Cmd+K → "Preparar meu PC"). | You can go in and install the rest whenever you want. Just installed something? Click "Check again". This screen won't come back on its own — to check again, go to Settings → Dependencies (or Ctrl/Cmd+K → "Set up my PC"). |
| setup.install | Instalar | Install |

> `Node.js` e `Git` (nomes) ficam idênticos.

`xlsx`:
| chave | PT-BR | EN |
|---|---|---|
| xlsx.empty_sheet | Planilha vazia. | Empty spreadsheet. |
| xlsx.no_sheets | Nenhuma planilha encontrada. | No spreadsheet found. |
| xlsx.read_only | somente leitura | read-only |
| xlsx.rows | linhas | rows |
| xlsx.truncated | mostrando {shownRows}×{shownCols} de {totalRows}×{totalCols} | showing {shownRows}×{shownCols} of {totalRows}×{totalCols} |

> `TldrawPanel.jsx`: **nenhuma string de UI** (a interface é do tldraw). Confirmar e não alterar.

- [ ] **Step 2: Migrar os três componentes** com `useT()`. No SetupScreen, mover `name`/`desc`/`note`/`level` das dependências para chaves e resolver no render (escopo de módulo). Variáveis do xlsx: `{shownRows}`, `{shownCols}`, `{totalRows}`, `{totalCols}`.

- [ ] **Step 3: Smoke + build + verificação visual** (aba API completa; tela de Setup; visualizador de planilha) nos dois idiomas.

- [ ] **Step 4: Commit**

```bash
git add src/components/ApiPanel.jsx src/components/SetupScreen.jsx src/components/XlsxViewer.jsx src/lib/locales/pt.json src/lib/locales/en.json
git commit -m "feat(i18n): API, tela de setup e visualizador de planilha"
```

---

## Task 9: Strings nativas do Electron (main.js + preload)

**Files:**
- Create: `main.i18n.cjs`
- Modify: `main.js`, `preload.js`, `scripts/i18n-parity.smoke.cjs`

**Interfaces:**
- Consumes: padrão de IPC existente (`notify:set`/`notify:get` em `main.js`), `loadConfig`/`saveConfig`, `safeSend`.
- Produces: `main.i18n.cjs` exportando `{ pt, en }`; IPC `lang:get`/`lang:set`; `window.api.getLang`/`window.api.setLang` no preload.

- [ ] **Step 1: Criar `main.i18n.cjs` (raiz, junto do main.js)**

```cjs
// Strings nativas do processo main (menus de contexto, diálogos, notificações).
// Ficam aqui (e não em src/lib/locales) porque o main é empacotado separado do
// renderer — importar de src/ no app distribuído é frágil. São poucas e estáveis.
module.exports = {
  pt: {
    ctx_back: 'Voltar',
    ctx_forward: 'Avançar',
    ctx_reload: 'Recarregar',
    ctx_cut: 'Recortar',
    ctx_copy: 'Copiar',
    ctx_paste: 'Colar',
    ctx_select_all: 'Selecionar tudo',
    ctx_copy_link: 'Copiar link',
    ctx_inspect: 'Inspecionar elemento',
    dialog_choose_folders: 'Escolha a(s) pasta(s) de projeto',
    notify_title: 'Carcará Code',
    notify_asking: 'Claude precisa de você em {name}',
    notify_done: 'Claude terminou em {name}',
  },
  en: {
    ctx_back: 'Go back',
    ctx_forward: 'Go forward',
    ctx_reload: 'Reload',
    ctx_cut: 'Cut',
    ctx_copy: 'Copy',
    ctx_paste: 'Paste',
    ctx_select_all: 'Select all',
    ctx_copy_link: 'Copy link',
    ctx_inspect: 'Inspect element',
    dialog_choose_folders: 'Choose project folder(s)',
    notify_title: 'Carcará Code',
    notify_asking: 'Claude needs you in {name}',
    notify_done: 'Claude finished in {name}',
  },
};
```

- [ ] **Step 2: Adicionar idioma + tradutor no topo do `main.js`**

Logo após o bloco `loadConfig`/`saveConfig` (linha ~56), adicionar:
```js
const NATIVE_STRINGS = require('./main.i18n.cjs');

// Idioma do processo main: config.json > idioma do sistema > 'pt'.
function currentLang() {
  const cfg = loadConfig();
  if (cfg.language === 'pt' || cfg.language === 'en') return cfg.language;
  return (app.getLocale() || '').toLowerCase().startsWith('pt') ? 'pt' : 'en';
}
// Traduz uma chave nativa no idioma atual, com interpolação de {tokens}.
function tn(key, vars) {
  const lang = currentLang();
  const dict = NATIVE_STRINGS[lang] || NATIVE_STRINGS.pt;
  let s = dict[key] != null ? dict[key] : (NATIVE_STRINGS.pt[key] != null ? NATIVE_STRINGS.pt[key] : key);
  if (vars) s = s.replace(/\{(\w+)\}/g, (m, k) => (k in vars ? String(vars[k]) : m));
  return s;
}
```

- [ ] **Step 3: Traduzir o menu de contexto da webview (main.js L152-164)**

Substituir os `label:` literais por `tn(...)`:
```js
Menu.buildFromTemplate([
  { label: tn('ctx_back'), enabled: contents.canGoBack(), click: () => contents.goBack() },
  { label: tn('ctx_forward'), enabled: contents.canGoForward(), click: () => contents.goForward() },
  { label: tn('ctx_reload'), click: () => contents.reload() },
  { type: 'separator' },
  { label: tn('ctx_cut'), role: 'cut', enabled: can.canCut },
  { label: tn('ctx_copy'), role: 'copy', enabled: can.canCopy },
  { label: tn('ctx_paste'), role: 'paste', enabled: can.canPaste },
  { label: tn('ctx_select_all'), role: 'selectAll' },
  ...(params.linkURL ? [{ type: 'separator' }, { label: tn('ctx_copy_link'), click: () => clipboard.writeText(params.linkURL) }] : []),
  { type: 'separator' },
  { label: tn('ctx_inspect'), click: () => { lastInspect = { x: params.x, y: params.y }; safeSend('devtools:toggle'); } },
]).popup();
```
> O menu é reconstruído a cada clique (`context-menu` event), então `tn()` lê o idioma atual sempre — não precisa rebuild manual ao trocar idioma.

- [ ] **Step 4: Traduzir a notificação (main.js L1156-1157)**

Trocar:
```js
const body = asking ? tn('notify_asking', { name }) : tn('notify_done', { name });
const n = new Notification({ title: tn('notify_title'), body });
```

- [ ] **Step 5: Traduzir o título do diálogo de pastas (main.js L384)**

Localizar a chamada `dialog.showOpenDialog` com `title: 'Escolha a(s) pasta(s) de projeto'` e trocar por `title: tn('dialog_choose_folders')`.

- [ ] **Step 6: Adicionar os handlers IPC de idioma**

Junto dos handlers `notify:get`/`notify:set` (linha ~1182), adicionar:
```js
ipcMain.handle('lang:get', () => ({ lang: currentLang() }));
ipcMain.handle('lang:set', (evt, { lang }) => {
  if (lang !== 'pt' && lang !== 'en') return { ok: false };
  const c = loadConfig(); c.language = lang; saveConfig(c);
  return { ok: true };
});
```

- [ ] **Step 7: Expor no `preload.js`**

Junto do bloco de notificações (linha ~58), adicionar:
```js
  getLang: () => ipcRenderer.invoke('lang:get'),
  setLang: (lang) => ipcRenderer.invoke('lang:set', { lang }),
```

- [ ] **Step 8: Estender o smoke test para cobrir as strings nativas**

Em `scripts/i18n-parity.smoke.cjs`, após `compare('renderer', pt, en);`, adicionar:
```cjs
const native = require('../main.i18n.cjs');
compare('native', native.pt, native.en);
```

- [ ] **Step 9: Smoke + build**

Run: `node scripts/i18n-parity.smoke.cjs` → `i18n parity ok`
Run: `npm run build` → sem erro

- [ ] **Step 10: Verificação manual**

Trocar o idioma na aba Idioma e confirmar: (a) menu de contexto do Preview (botão direito) muda de idioma; (b) o diálogo "Adicionar projeto" abre com o título traduzido; (c) uma notificação de "Claude terminou" sai no idioma escolhido (deixar um projeto Claude rodando fora de foco).

> Lembrete: alterações no `main.js`/`preload.js` exigem reiniciar o processo Electron para valer — **não** force o relaunch se houver sessão Claude viva; combine com o usuário.

- [ ] **Step 11: Commit**

```bash
git add main.i18n.cjs main.js preload.js scripts/i18n-parity.smoke.cjs
git commit -m "feat(i18n): menus, dialogos e notificacoes nativas (Electron)"
```

---

## Self-Review (cobertura do spec)

- **Engine caseira sem dependência** → Task 1 ✓
- **Dois idiomas pt/en, JSON, fallback, interpolação** → Task 1 (`i18n.jsx`) ✓
- **Padrão = idioma do sistema** → `detectInitial()` (renderer) + `currentLang()` (main) ✓
- **Aba "Idioma" nas Configurações** → Task 2 ✓
- **Troca imediata sem reabrir** → estado React re-renderiza; `useEffect` empurra pro main ✓
- **Toda a UI React traduzida** → Tasks 2–8 cobrem todos os componentes com strings (inventário dos 6 agentes) ✓
- **Menus/diálogos/notificações nativas** → Task 9 ✓
- **Persistência localStorage + userData** → Task 1 (localStorage) + Task 9 (`cfg.language`) ✓
- **Jargão/nomes próprios preservados** → regra aplicada nas tabelas (Git/commit/MCP/API/Preview/terminal e nomes de marca idênticos) ✓
- **Smoke de paridade** → Task 1 cria, Task 9 estende para nativas ✓
- **Componentes não-funcionais (ErrorBoundary class, arrays de módulo em Settings/Setup)** → tratados com `tStatic`/chave-no-render ✓

**Consistência de tipos/nomes:** `useT()` devolve `t(key, vars?)` usado uniformemente; `useLang()` → `{ lang, setLang }` usado na Task 2; `window.api.setLang(lang)` (preload, Task 9) casa com a chamada em `i18n.jsx` (Task 1) e com o handler `lang:set` (Task 9). `tn(key, vars)` no main casa com `main.i18n.cjs`. Sem divergência de assinaturas.

**Observação de execução:** as tabelas de strings vêm do inventário automático; alguns números de linha são aproximados. O implementador deve localizar cada literal pelo texto (não pela linha) e confirmar que cobriu o arquivo inteiro antes de fechar cada task.
